#include <iostream>
using namespace std;
//Abulqasim Mellim chestno skaju ya ne snog sdelat dz ono bilo ochen trudnim
// ya smog prosto zapisat dla 2 zadaniya perevod chasa k minutam i sekundam


int main()
{
	/*int num = 0;
	cout << "Enter number: ", cin >> num;
	int hour = num / 3600;
	int minutes = (num - (hour * 3600)) / 60;
	int seconds = (num - (hour * 3600)) - (minutes * 60);
	cout << hour << " h " << minutes << " m " << seconds << 
		if (num=2)
		{
			cout << num1 << endl;*/
		}
}

